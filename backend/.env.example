DATABASE_URL="postgresql://postgres:password@localhost:5432/alr_facturacion"
JWT_SECRET="cambiar-esto-en-produccion"
JWT_EXPIRES_IN="7d"
PORT=3001

# Email (SMTP)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="facturacion@alr.cl"
SMTP_PASS="app-password-aqui"
SMTP_FROM="Facturación ALR <facturacion@alr.cl>"

# Storage (para PDFs)
STORAGE_PATH="./storage"

# Frontend URL (para CORS)
FRONTEND_URL="http://localhost:5173"
